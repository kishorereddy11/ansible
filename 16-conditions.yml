- name: install nginx
  hosts: local
  connection: local
  tasks:
  - name: install nginx in RHEL
    ansible.builtin.dnf:
      name: nginx
      state: installed
    when: ansible_os_family == "RedHat"