- name: install nginx
  hosts: local
  connection: local
  tasks:
  - name: install nginx in RHEL
    ansible.builtin.debug:
      name: nginx
      state: installed
    when: ansible_os_family == "RedHat"